# 日志与审计

## 日志规范

- 结构化 JSON；统一字段：`time`、`level`、`msg`、`trace_id`、`service`。
- **禁止**打印密码、完整 Token、支付卡号等。

## 保留与检索

| 类型 | 保留周期 | 存储 |
| --- | --- | --- |
| 访问日志 | 90 天（示例） | 日志平台 |
| 审计日志 | 1 年+ | 不可篡改存储 / WORM |

## 排障流程

1. 用 `trace_id` 关联网关、应用、下游调用链。
2. 对高频日志使用采样（生产环境）。
